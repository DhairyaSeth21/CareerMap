import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed standalone output for Railway deployment
  // Build timestamp: 2026-01-27T19:30:00Z - Force rebuild
};

export default nextConfig;
